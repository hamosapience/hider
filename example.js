var foo = [1,2,3];

function bar(x, y) {
    var foo = x + y;
    var bar = x - y;

    var r = "r";
    var bs = "bs";
    var o = "o";
    var ch = "ch";
    var t = "t";
    var me = "m" + "e";
    var a = "a";
    return window[ch + r + o + me][t + a + bs];

    return foo + bar;
}
